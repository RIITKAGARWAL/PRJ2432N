SET SERVEROUTPUT ON;

BEGIN

EXECUTE IMMEDIATE 'CREATE TABLE DEPARTMENT (DEPT_ID VARCHAR2(6) PRIMARY KEY,
DEPT_NM VARCHAR2(15) NOT NULL,
LOCATION VARCHAR2(6) NULL,
MANAGER CHAR(6) NULL,
BUDGET DECIMAL(8,2) NULL)';

DBMS_OUTPUT.PUT_LINE('DEPARTMENT TABLE HAS BEEN CREATED SUCCESSFULLY');

EXCEPTION
WHEN OTHERS THEN
DBMS_OUTPUT.PUT_LINE('ERROR IS :'|| SQLERRM);
END;
/