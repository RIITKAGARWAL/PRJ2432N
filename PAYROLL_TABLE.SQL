SET SERVEROUTPUT ON;

BEGIN
   EXECUTE IMMEDIATE 'CREATE TABLE PAYROLL (
   PRL_ID VARCHAR2(6) PRIMARY KEY,
   EMP_ID VARCHAR2(6) CONSTRAINT FK_EMPL REFERENCES EMPLOYEE(EMP_ID),
   PAY_PERIOD DATE NOT NULL ,
	TOT_EARN DECIMAL(7,2) DEFAULT 0,
   TOT_DEDUCT DECIMAL(7,2) DEFAULT 0,
   NET_SAL DECIMAL(7,2) NOT NULL,
   PMT_DT DATE NULL,
   PMT_STATUS CHAR(1) DEFAULT ''U'' NOT NULL CHECK (PMT_STATUS IN (''P'', ''U''))
)';

   DBMS_OUTPUT.PUT_LINE('PAYROLL TABLE HAS BEEN CREATED SUCCESSFULLY');
EXCEPTION
   WHEN OTHERS THEN
      DBMS_OUTPUT.PUT_LINE('ERROR: ' || SQLERRM);
END;
/